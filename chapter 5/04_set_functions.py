set1={10,20,30,40,50}

# set1.add(99) # add new element 

# print(set1,type(set1))

# set1.pop() # this pops any element present in the set because set is unordered

# set1.discard(70) # this is used to avoid key error and wroks same as remove

# set1.remove(40) # this works same as discard but programmers avoid because it returns key error if element is not in the set

